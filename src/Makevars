# Empty Makevars: all build logic is handled by the configure script in the package root.

# GLib include and library paths using pkg-config for cross-platform compatibility
PKG_CPPFLAGS = $(shell pkg-config --cflags glib-2.0)
PKG_LIBS = $(shell pkg-config --libs glib-2.0)
